<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.CarDAO">
	
	<insert id="insert" parameterType="car">
		INSERT INTO CAR VALUES (C_SEQ.NEXTVAL,#{userid},#{carnum},#{carname},#{cartype},#{carmodel},#{caryear},#{carimg},#{caroiltype},#{tablettoken})
	</insert>
	
	<update id="update" parameterType="car">
		UPDATE CAR SET CARNUM=#{carnum}, CARNAME=#{carname}, CARIMG=#{carimg}, TABLETTOKEN=#{tablettoken} WHERE CARID=#{carid}
	</update>
	
	<delete id="delete" parameterType="Integer">
		DELETE FROM CAR WHERE CARID=#{k}
	</delete>
	
	<select id="select" parameterType="Integer" resultType="car">
		SELECT * FROM CAR WHERE CARID=#{k}
	</select>
	
	<select id="selectall" resultType="car">
		SELECT * FROM CAR
	</select>
	
	
</mapper>